# Payment simulator notice

With `MPESA_ENABLED=false`, payment requests are simulated. No real funds are charged or received. A simulator-confirmed payment is test data, not proof of payment. Verify the payment routes and controller are wired to the simulator endpoints before testing; use a staging database and test accounts.
